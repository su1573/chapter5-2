spring:
  application:
    name: service-B
#使用本地config-server仓库配置文件  应用名-dev1
#  cloud:
#    config:
#      fail-fast: true
#      discovery:
#        enabled: true
#        service-id: config-server
#  profiles:
#    active: dev1

#调用chinare-cloud-config-server配置中心，从数据库中获取service-B1的配置
  cloud:
    config:
      label: test1
      profile: dev
      discovery:
        enabled: true #开启可用
        serviceId: chinare-cloud-config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
    registry-fetch-interval-seconds: 30



